# ORD-002 - Testes RED do fluxo principal de compra

Data: 2026-03-27

## Objetivo
Registrar evidência do ciclo RED da task `ORD-002` para o use-case `createOrder`, cobrindo cenários críticos de compra definidos no AGENTS.

## Escopo executado
- Criação da suíte unitária de aplicação:
  - `tests/unit/application/create-order.use-case.test.ts`
- Cenários cobertos na suíte RED:
  - cálculo server-side do total com preço do lote (ignorando manipulação do client)
  - rejeição por estoque insuficiente
  - rejeição por lote fora da janela de venda
  - aplicação de cupom válido
  - rejeição de cupom inválido (janela/contexto)

## Evidência RED
Comando:
```bash
npm run test:unit -- tests/unit/application/create-order.use-case.test.ts
```

Resultado observado:
- 1 arquivo de teste falhou.
- 5 testes falharam.
- Motivo esperado: ausência de implementação/export `createCreateOrderUseCase` em `src/server/application/use-cases/create-order.use-case.ts`.

## Verificações adicionais
Comandos:
```bash
npm run test:unit -- tests/unit/server/api/create-order.handler.test.ts
npm run test:unit -- tests/unit/server/api/schemas/create-order.schema.test.ts
npm run lint
npm run build
```

Resultado observado:
- `create-order.handler.test.ts`: 4 testes passando.
- `create-order.schema.test.ts`: 2 testes passando.
- `npm run lint`: passou sem erros.
- `npm run build`: completou com sucesso.

## Próximo passo natural
- Executar `ORD-003`: implementar `createOrder` mínimo para GREEN, fazendo os testes RED desta task passarem sem mover regra para handler/UI.
