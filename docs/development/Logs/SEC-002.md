# SEC-002 - Regressões iniciais de estoque/estado

Data: 2026-03-27

## Objetivo
Cobrir regressões iniciais de estoque e estado para reduzir risco antes da fase de `createOrder`.

## Escopo executado
- Suíte dedicada de regressão criada em `tests/regression/stock-and-state.regression.test.ts`.
- Cobertura de cenários críticos:
  - oversell por quantidade acima do estoque disponível;
  - ticket ativo inválido quando o pedido está expirado.
- Inclusão da suíte no gate padrão via `npm run test:regression` e encadeamento em `npm run test`.

## Evidência RED
Comando:
```bash
npm run test:regression
```

Resultado:
- Falha reproduzível antes do fix.
- Motivo: script inexistente no `package.json` (`Missing script: "test:regression"`).

## Correção mínima aplicada
- `package.json`
  - adicionado script `test:regression`;
  - `test` atualizado para executar `unit -> regression -> integration`.
- `tests/regression/stock-and-state.regression.test.ts`
  - adicionados 2 testes de regressão de estoque/estado.

## Evidência GREEN
Comando:
```bash
npm run test:regression
```

Resultado:
- 1 arquivo de teste passou.
- 2 testes passaram.

## Observação
Esta evidência local complementa a atualização de status da task `SEC-002` na fase 002.
