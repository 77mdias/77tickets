# ORD-004 - Cobrir regressões de pedido expirado e estado inválido

Data: 2026-03-28

## Objetivo
Concluir a task `ORD-004` cobrindo regressões de estado para garantir que pedido expirado não mantenha ticket ativo e que transições inválidas de status de pedido continuem bloqueadas no domínio.

## Escopo executado
- Nova suíte de regressão dedicada em:
  - `tests/regression/orders/order-state.regression.test.ts`
- Ajuste mínimo no domínio para regra explícita e reutilizável:
  - `src/server/domain/orders/order.rules.ts`
  - `src/server/domain/tickets/ticket.rules.ts`
- Comportamentos cobertos:
  - pedido `expired` não é elegível para manter ticket ativo
  - validação de check-in continua rejeitando ticket ativo com pedido não pago
  - transição inválida `paid -> expired` continua rejeitada

## Evidência TDD (RED -> GREEN)
Comando RED (antes do ajuste de domínio):
```bash
npm run test:regression
```

Resultado RED observado:
- Falha em `tests/regression/orders/order-state.regression.test.ts`.
- Motivo: `TypeError: isOrderStatusEligibleForActiveTicket is not a function`.

Comando GREEN (após ajuste mínimo):
```bash
npm run test:regression
```

Resultado GREEN observado:
- `2` arquivos de regressão passando.
- `4` testes de regressão passando.

## Validações executadas
```bash
npm run test:unit -- tests/unit/server/domain/orders/order.rules.test.ts tests/unit/server/domain/tickets/ticket.rules.test.ts
npm run test:regression
npm run lint
npm run build
```

Resultado observado:
- `test:unit` (domínio orders/tickets): ✅ 34 passing
- `test:regression`: ✅ 4 passing
- `lint`: ✅ sem erros
- `build`: ✅ sucesso

## Atualizações operacionais
- Task `ORD-004` marcada como concluída em `docs/development/TASKS/PHASE-003-create-order-flow.md`.
- Critérios de aceite de `ORD-004` marcados como atendidos.
- Progresso da Fase 003 atualizado para `4/11` em:
  - `docs/development/TASKS/PHASE-003-create-order-flow.md`
  - `docs/development/TASKS.md`
- Registro no `CHANGELOG` em `[Unreleased]`.

## Riscos remanescentes
- A validação de transição permanece no domínio e ainda depende de adoção pelos próximos fluxos de atualização de status (`API-002+`).
