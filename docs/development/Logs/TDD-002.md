# TDD-002 - Primeiros testes para contratos e schemas

Data: 2026-03-26

## Objetivo
Registrar a evidência do ciclo RED -> GREEN para a task `TDD-002`.

## Escopo executado
- Teste de schema para rejeitar campos desconhecidos na fronteira do handler.
- Teste de contratos de repositório para garantir ausência de dependências de framework/ORM.

## Evidência RED
Comando:
```bash
npm run test:unit -- tests/unit/server/api/schemas/create-order.schema.test.ts tests/unit/server/repositories/repository-contracts.test.ts
```

Resultado:
- 1 teste falhou.
- Falha esperada em `createOrderSchema rejects payload with unknown fields at the handler boundary`.
- Motivo: `createOrderSchema.safeParse(...)` retornava sucesso para payload com `clientTotalInCents`, mostrando que a fronteira ainda aceitava campos extras.

## Correção mínima aplicada
- `src/server/api/schemas/create-order.schema.ts`
- Mudança: `z.object({...}).strict()` para rejeitar chaves não reconhecidas na entrada externa.

## Evidência GREEN
Mesmo comando reexecutado após a correção:
```bash
npm run test:unit -- tests/unit/server/api/schemas/create-order.schema.test.ts tests/unit/server/repositories/repository-contracts.test.ts
```

Resultado:
- 2 arquivos de teste passaram.
- 2 testes passaram.

## Observação
A evidência foi registrada neste log técnico local para substituir a referência a PR enquanto o trabalho permanece no repositório local.
