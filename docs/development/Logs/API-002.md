# API-002 - Persistência transacional de pedido/itens/tickets

Data: 2026-03-28

## Objetivo
Concluir a task `API-002` garantindo atomicidade de escrita para `orders`, `order_items` e `tickets`, evitando gravações parciais em caso de falha e mantendo a arquitetura por camadas.

## Escopo executado
- Migração do client Drizzle para driver com transação:
  - `src/server/infrastructure/db/client.ts` (`drizzle-orm/neon-serverless` + `Pool`).
  - `tests/integration/setup/global-setup.ts` (migrator `drizzle-orm/neon-serverless/migrator`).
- Persistência transacional no repositório:
  - `src/server/repositories/order.repository.contracts.ts` com suporte a tickets na escrita de pedido.
  - `src/server/repositories/drizzle/drizzle-order.repository.ts` persistindo `order -> items -> tickets` dentro de `db.transaction(...)`.
- Ajuste do use-case para gerar tickets server-side:
  - `src/server/application/use-cases/create-order.use-case.ts`.
  - geração de um ticket por unidade comprada, com código server-side.
- Cobertura de testes:
  - `tests/unit/application/create-order.use-case.test.ts`.
  - `tests/integration/repositories/drizzle-order.repository.integration.test.ts`.
  - `tests/unit/server/infrastructure/db-client.test.ts`.

## Evidência TDD (RED -> GREEN)
Comando RED (antes da implementação completa):
```bash
npm run test:unit -- tests/unit/application/create-order.use-case.test.ts
```

Resultado RED observado:
- 1 teste falhando.
- Motivo: payload de tickets não estava sendo enviado para a persistência (`persistedTickets` era `undefined`).

Comando GREEN (após implementação mínima):
```bash
npm run test:unit -- tests/unit/application/create-order.use-case.test.ts tests/unit/server/infrastructure/db-client.test.ts
```

Resultado GREEN observado:
- 2 arquivos de teste passando.
- 8 testes passando.

## Validações executadas
```bash
npm run test:unit
npm run test:regression
npm run lint
npm run build
npm run test:integration
```

Resultado observado:
- `test:unit`: ✅ 14 arquivos, 126 testes passando
- `test:regression`: ✅ 2 arquivos, 4 testes passando
- `lint`: ✅ sem erros
- `build`: ✅ sucesso
- `test:integration`: ⚠️ falha de ambiente (`TEST_DATABASE_URL` configurada, mas banco inacessível no setup)

## Atualizações operacionais
- Task `API-002` marcada como concluída em `docs/development/TASKS/PHASE-003-create-order-flow.md`.
- Critérios de aceite de `API-002` marcados como atendidos.
- Progresso da Fase 003 atualizado para `6/11` em:
  - `docs/development/TASKS/PHASE-003-create-order-flow.md`
  - `docs/development/TASKS.md`
- Registro no `CHANGELOG` em `[Unreleased]`.

## Nota de idempotência
- Critério "idempotente quando aplicável" foi tratado como **N/A na fase 003**.
- Não existe chave de idempotência no contrato atual (`create-order.schema.ts`).
- A limitação foi explicitamente documentada para a próxima evolução (API-003/API-004+).

## Riscos residuais
- A cobertura de integração da atomicidade não pôde ser executada localmente por indisponibilidade de conexão com o banco de testes.
- Chave de idempotência ainda não implementada no endpoint de checkout.
