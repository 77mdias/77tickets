# CHK-003 - Implementar `validateCheckin` até GREEN

Data: 2026-03-29

## Objetivo
Concluir a task `CHK-003` implementando o use-case `validateCheckin` para validar elegibilidade de check-in e consumo único do ticket com rastreabilidade mínima de auditoria.

## Escopo executado
- Implementação do use-case em:
  - `src/server/application/use-cases/validate-checkin.use-case.ts`
- Export do novo use-case em:
  - `src/server/application/use-cases/index.ts`
- Comportamentos implementados:
  - rejeição de ticket inexistente (`ticket_not_found`)
  - rejeição de mismatch de evento (`event_mismatch`)
  - rejeição de ticket usado (`ticket_used`)
  - rejeição de ticket cancelado (`ticket_cancelled`)
  - rejeição de pedido não elegível (`order_not_eligible`)
  - aprovação + marcação de uso (`markAsUsed`) para ticket ativo com pedido elegível
  - retorno com metadados de auditoria (`ticketId`, `eventId`, `checkerId`, `validatedAt`)

## Evidência TDD (RED -> GREEN)
Comando RED (antes da implementação):
```bash
npx vitest run --config vitest.config.ts tests/unit/application/validate-checkin.use-case.test.ts
```

Resultado RED observado:
- 5/5 testes falhando.
- Motivo: módulo `validate-checkin.use-case` ausente.

Comando GREEN (após implementação):
```bash
npx vitest run --config vitest.config.ts tests/unit/application/validate-checkin.use-case.test.ts
```

Resultado GREEN observado:
- 5/5 testes passando.

## Validações executadas
```bash
npx vitest run --config vitest.config.ts tests/unit/application/validate-checkin.use-case.test.ts
npm run test:unit
npm run lint
npm run build
```

Resultado observado:
- `validate-checkin.use-case.test.ts`: ✅ 5 passing
- `test:unit`: ✅ 152 passing
- `lint`: ✅ sem erros
- `build`: ✅ sucesso

## Atualizações operacionais
- Task `CHK-003` marcada como concluída em `docs/development/TASKS/PHASE-004-ticket-checkin-rbac.md`.
- Critérios de aceite da `CHK-003` marcados como atendidos.
- Progresso da fase 004 atualizado para `3/9` em:
  - `docs/development/TASKS/PHASE-004-ticket-checkin-rbac.md`
  - `docs/development/TASKS.md`
- Registro em `[Unreleased]` no `docs/development/CHANGELOG.md`.

## Riscos remanescentes
- A proteção contra corrida concorrente de duas validações simultâneas depende da task `OPS-002` (update condicional/atômico no repositório).
- Endpoint operacional e mapeamento HTTP de erros ainda pendentes (`OPS-001`).

## Próximo passo natural
- Executar `OPS-001` para expor `validateCheckin` via handler com contratos de erro estáveis.
