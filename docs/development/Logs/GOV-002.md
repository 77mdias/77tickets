# GOV-002 — Executar QA funcional de ciclo de gestão de evento

**Data:** 2026-03-29  
**Status:** ✅ Concluído  
**Fase:** 005

---

## Objetivo

Homologar o ciclo funcional de gestão de evento (publish/update e governança de cupons) com evidência de comportamento permitido e bloqueado por perfil, sem abrir bloqueadores críticos para encerramento da fase.

---

## Escopo de QA executado (híbrido)

- Evidência automatizada de RBAC/ownership e cenários de erro:
  - `tests/integration/api/events/auth.test.ts`
  - `tests/integration/api/coupons/auth.test.ts`
- Evidência de prontidão operacional do repositório:
  - `npm run lint`
  - `npm run build`

---

## Checklist funcional com evidência

1. Publish de evento bloqueia perfis não autorizados (`customer`, `checker`) e organizer fora do ownership.
2. Publish de evento permite `organizer` no próprio escopo e `admin` global.
3. Update de status de evento bloqueia perfis não autorizados e organizer fora do ownership.
4. Update de status de evento permite `organizer` no próprio escopo e `admin` global.
5. Create/Update de cupom bloqueia `customer`/`checker` e organizer cross-owner.
6. Create/Update de cupom permite `organizer` owner e `admin` global.
7. Erro de conflito funcional em cupom inválido mantém `reason` estável (`invalid_coupon_window`).
8. Build e lint do projeto sem falhas após homologação.

---

## Evidências de execução

```bash
npm run test:integration -- tests/integration/api/events/auth.test.ts tests/integration/api/coupons/auth.test.ts
# Test Files 2 passed (2)
# Tests 19 passed (19)

npm run lint
# eslint sem erros

npm run build
# vinext build concluído com sucesso
```

---

## Critérios de aceitação GOV-002

- [x] Checklist completo com evidência anexada.
- [x] Nenhum bloqueador crítico aberto.

---

## Resultado e riscos residuais

- Resultado: fluxo de gestão homologado no escopo da fase 005 para perfis distintos e cenários de erro principais.
- Riscos residuais:
  - A task `GOV-003` ainda pendente para encerramento formal da fase (documentação/handoff final).
